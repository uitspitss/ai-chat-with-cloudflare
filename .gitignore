node_modules/
dist/
*.tsbuildinfo
.DS_Store
.playwright-mcp/
.claude/*.log
.claude/sessions/
.claude/settings.local.json
.claude/team-state.md

# monorepo
.wrangler/
.turbo/

# expo
.expo/
apps/mobile/expo-env.d.ts
# CNG（prebuild で生成される）。ネイティブの設定は app.json と config plugin が持つ
apps/mobile/ios/
apps/mobile/android/

# wrangler のローカルシークレット。秘密はリポジトリに置かない
# （secrets:pull の中間ファイル .dev.vars.tmp も含めるので * を付ける）
.dev.vars*
.env
.env.*

# Playwright
# monorepo ではスラッシュを含むパターンが .gitignore の位置起点になるので **/ を付ける
test-results/
playwright-report/
blob-report/
**/playwright/.cache/
**/e2e/.auth/

*storybook.log
storybook-static
